Static validation — COMPLETE_STABLE_FIXED13_SPARK_JOBS_UI

Result: 0 error(s), 1 warning(s), 208 passed checks

Validated contracts include:
- Compose YAML and the exact 25-service topology
- localhost-only published ports
- explicit healthchecks, including spark-jupyter
- Spark 4 / Delta / Iceberg / S3 classpath constraints
- imported Spark configuration and S3 event logging
- JupyterLab 4.4.10 exact dependency pin
- shared Jupyter/Spark Python and PySpark runtime
- persistent notebook bind mount and valid starter notebook
- generated Jupyter access token contract
- writable `/home/spark` and explicit Jupyter/XDG runtime paths
- explicit PySpark/Py4J `PYTHONPATH` and a build-tested stable Py4J alias
- explicit non-root `spark` user, recursive ownership and real notebook writes
- Spark Jobs UI owned by `spark-jupyter` and published on configurable localhost port 4040
- installer and Windows launcher checks for the live Spark Jobs mapping/session
- no obsolete Spark UI hostnames in the notebooks
- Python, Bash and PowerShell 5.1 structural syntax
- Kafka, PostgreSQL, object-store, Trino, Airflow and Elastic contracts
- training datasets and Spark jobs
- exact SHA-256 manifest coverage for every distribution file

Environment warning:
Docker CLI/daemon is unavailable in the authoring environment. Runtime
container validation is therefore performed by Install-BigDataLab.ps1 on the
target Docker Desktop host before it can report installation success.
